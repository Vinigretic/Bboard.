1. For start project:

    pip install django

2. For create models:

   pip install pillow
   pip install django-cleanup 
   pip install easy-thumbnails

3. For create templates

   pip install django-bootstrap4

4. For create captcha

   pip install django-simple-captcha

5. For storing secret keys and passwords:

    pip install python-decouple



